spring:
  application:
    name: status-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/statusdb
    username: root
    password: 36133520
  jpa:
    hibernate:
      ddl-auto: update
