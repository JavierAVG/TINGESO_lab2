apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config-map
  labels:
    app: mysql-config-map
data:
  DB_HOST_CLIENTS: client-db-service
  DB_PORT_CLIENTS: "3306"
  DB_NAME_CLIENTS: clientsdb
  DB_HOST_LOANS: loan-db-service
  DB_PORT_LOANS: "3306"
  DB_NAME_LOANS: loansdb
  DB_HOST_STATES: state-db-service
  DB_PORT_STATES: "3306"
  DB_NAME_STATES: statesdb